CONSTRUCT_API_KEY=replace-with-the-one-time-target-key
# Recognized internal dogfood endpoints also select the exact control plane
# permitted to frame this host. Unknown origins fail the build.
CONSTRUCT_INTERNAL_PLATFORM_API_URL=http://127.0.0.1:4100
